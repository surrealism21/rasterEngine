#pragma once

enum class ActorID {
    // Custom entries go below this bar
    // --------------------------------



    // System actors (generally, don't tamper with these!)
    LoadScreen = -1,

    Num
};